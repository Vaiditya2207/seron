import * as raycast from '@raycast/api';
export declare namespace Clipboard {
    const copy: typeof raycast.Clipboard.copy;
    const paste: typeof raycast.Clipboard.paste;
    const read: typeof raycast.Clipboard.read;
    const readText: typeof raycast.Clipboard.readText;
    const clear: typeof raycast.Clipboard.clear;
}
